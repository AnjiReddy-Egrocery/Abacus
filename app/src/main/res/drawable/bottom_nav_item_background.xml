<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:state_checked="true">
        <layer-list>
            <item>
                <shape android:shape="oval">
                    <solid android:color="#FF6600" /> <!-- Circle color -->
                    <size
                        android:width="30dp"
                        android:height="30dp" /> <!-- Circle size -->
                </shape>
            </item>
        </layer-list>
    </item>
    <!-- Default state (no effect) -->
    <item>
        <shape android:shape="oval">
            <solid android:color="@android:color/transparent" /> <!-- Completely transparent -->
        </shape>
    </item>

</selector>